/**
 * @file  Benchmark.cpp
 * @brief Implementation translation unit.
 *
 * Benchmark is a header-only template class; this file exists to satisfy
 * the curriculum's .h / .cpp separation rule and to hold any future
 * non-template helpers.
 */

#include "Benchmark.h"

// All template methods are defined in Benchmark.h.
// Non-template free functions would go here.

namespace dsa {
    // (intentionally empty for now)
} // namespace dsa